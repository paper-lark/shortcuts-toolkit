import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("This app provides actions for Apple Shortcuts")
        }
        .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
